{
  "name": "slurp",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present && npm run test:e2e:support",
    "test:e2e:support": "jest --config jest.e2e.config.js --runInBand",
    "test:e2e": "firebase emulators:exec --project slurp-e2e --only auth,firestore \"npx playwright test\"",
    "test:e2e:ui": "firebase emulators:exec --project slurp-e2e --only auth,firestore \"npx playwright test --ui\"",
    "test:e2e:debug": "firebase emulators:exec --project slurp-e2e --only auth,firestore \"npx playwright test --debug\"",
    "test:e2e:services": "node scripts/run-e2e-services.mjs",
    "test:e2e:install": "npx playwright install chromium"
  },
  "engines": {
    "node": ">=20",
    "npm": ">=11"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.0.0",
    "eslint": "^8.57.0",
    "firebase-tools": "15.28.1",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.4",
    "tsx": "^4.0.0",
    "typescript": "^5.4.0"
  }
}
